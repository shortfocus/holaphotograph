{
  "name": "camera-review-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create camera-review-db",
    "db:migrate": "wrangler d1 migrations apply camera-review-db",
    "db:migrate:remote": "wrangler d1 migrations apply camera-review-db --remote",
    "db:local": "wrangler d1 execute camera-review-db --local --file=./migrations/0001_init.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250210.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.66.0"
  },
  "dependencies": {}
}
