# OG 이미지 (소셜 공유용)

링크 공유 시 미리보기로 쓰이는 이미지입니다.

- **경로**: `public/og-image.png`
- **권장 크기**: **1200×630** (비율 1.91:1). 이 크기가 아니면 SNS에서 잘리거나 깨져 보일 수 있음.
- **형식**: PNG 또는 JPG. 파일명이 `og-image.png`가 아니면 `Layout.astro`의 `ogImage` 변수를 해당 경로로 수정하면 됨.

이미지가 없으면 공유 시 미리보기가 비어 있거나 캐시된 예전 이미지가 보일 수 있습니다.
