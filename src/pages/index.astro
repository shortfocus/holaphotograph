---
import Layout from '../layouts/Layout.astro';
---

<Layout>
  <!-- 2. 히어로 섹션 (Brand Positioning) - 단일, 좌측 텍스트 / 우측 영상 -->
  <section class="hero-section relative min-h-screen overflow-hidden bg-[#0F172A] pt-[4rem]">
    <div class="hero-glow absolute inset-0 bg-[radial-gradient(ellipse_80%_50%_at_50%_-20%,rgba(225,29,72,0.1),transparent)] pointer-events-none" aria-hidden="true" />
    <div class="hero-noise absolute inset-0 pointer-events-none" aria-hidden="true" />

    <div class="relative mx-auto flex min-h-[calc(100vh-4rem)] max-w-7xl flex-col items-center justify-center gap-8 px-4 py-24 sm:px-6 lg:flex-row lg:gap-16 lg:px-8">
      <div class="flex-1 text-center lg:text-left">
        <span class="hero-animate inline-block rounded-full border border-[#E11D48]/40 bg-[#E11D48]/10 px-4 py-1.5 text-xs font-medium uppercase tracking-wider text-red-300" style="animation-delay: 0ms; opacity: 0">Camera Review Archive</span>
        <h1 class="hero-animate mt-6 text-3xl font-bold leading-tight tracking-tight text-white sm:text-4xl md:text-5xl lg:text-6xl" style="animation-delay: 100ms; opacity: 0">
          카메라 선택부터 촬영 노하우까지<br />
          <span class="text-[#F43F5E]">실전 기반 리뷰 플랫폼</span>
        </h1>
        <p class="hero-animate mx-auto mt-4 max-w-xl text-base leading-relaxed text-slate-400 sm:text-lg lg:mx-0 lg:text-xl" style="animation-delay: 200ms; opacity: 0">장비 비교 · 실사용 테스트 · 구매 가이드</p>
        <div class="hero-animate mt-8 flex flex-wrap items-center justify-center gap-4 lg:justify-start" style="animation-delay: 300ms; opacity: 0">
          <a href="https://www.youtube.com/channel/UCsD5VP6TvRMt-sq0q25HlIA" target="_blank" rel="noopener noreferrer" class="inline-flex items-center gap-2 rounded-xl bg-[#E11D48] px-6 py-3 text-base font-semibold text-white shadow-lg shadow-[#E11D48]/25 transition-all hover:scale-105 hover:bg-[#be123c] hover:shadow-[#E11D48]/40">
            <svg class="h-5 w-5" viewBox="0 0 24 24" fill="currentColor"><path d="M23.498 6.186a3.016 3.016 0 0 0-2.122-2.136C19.505 3.545 12 3.545 12 3.545s-7.505 0-9.377.505A3.017 3.017 0 0 0 .502 6.186C0 8.07 0 12 0 12s0 3.93.502 5.814a3.016 3.016 0 0 0 2.122 2.136c1.871.505 9.376.505 9.376.505s7.505 0 9.377-.505a3.015 3.015 0 0 0 2.122-2.136C24 15.93 24 12 24 12s0-3.93-.502-5.814zM9.545 15.568V8.432L15.818 12l-6.273 3.568z"/></svg>
            유튜브에서 보기
          </a>
          <a href="#youtube-long" class="inline-flex items-center gap-2 rounded-xl border-2 border-white/30 bg-white/5 px-6 py-3 text-base font-semibold text-white backdrop-blur transition-all hover:border-white/50 hover:bg-white/10">
            최신 리뷰 보기
          </a>
        </div>
      </div>
      <div class="hero-animate flex-1 min-w-0" style="animation-delay: 150ms; opacity: 0">
        <div class="overflow-hidden rounded-2xl shadow-2xl shadow-black/30 ring-1 ring-white/10">
          <iframe src="https://www.youtube.com/embed/jMMTLjQLTWg?rel=0&autoplay=1&mute=1" title="올라포토 유튜브 영상" class="aspect-video w-full min-h-[200px] sm:min-h-[280px]" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen />
        </div>
      </div>
    </div>
  </section>

  <!-- 3. 전문가 소개 섹션 (Authority Builder) -->
  <section id="expert" class="section-spacing bg-[#F8FAFC] py-24">
    <div class="mx-auto max-w-6xl px-4 sm:px-6">
      <h2 class="section-title mb-10 text-2xl font-bold text-[#111827] sm:text-3xl">전문가 소개</h2>
      <div class="flex flex-col items-center gap-12 lg:flex-row lg:items-start lg:gap-16">
        <div class="flex-shrink-0">
          <div class="h-48 w-48 overflow-hidden rounded-2xl bg-slate-200 sm:h-56 sm:w-56">
            <img src="/favicon.svg" alt="올라포토" class="h-full w-full object-cover object-center opacity-90" />
          </div>
        </div>
        <div class="flex-1 text-center lg:text-left">
          <ul class="space-y-4 text-[#6B7280]">
            <li class="flex items-start gap-3">
              <span class="mt-1.5 h-2 w-2 flex-shrink-0 rounded-full bg-[#E11D48]" />
              <span>카메라 리뷰 전문 채널 운영</span>
            </li>
            <li class="flex items-start gap-3">
              <span class="mt-1.5 h-2 w-2 flex-shrink-0 rounded-full bg-[#E11D48]" />
              <span>유튜브 구독자 <strong id="stats-expert-subs" class="text-[#111827]">2,550</strong>명</span>
            </li>
            <li class="flex items-start gap-3">
              <span class="mt-1.5 h-2 w-2 flex-shrink-0 rounded-full bg-[#E11D48]" />
              <span>누적 블로그 방문자 <strong id="stats-expert-blog" class="text-[#111827]">65만+</strong></span>
            </li>
            <li class="flex items-start gap-3">
              <span class="mt-1.5 h-2 w-2 flex-shrink-0 rounded-full bg-[#E11D48]" />
              <span>실사용 기반 장비 분석 콘텐츠 제작</span>
            </li>
          </ul>
          <a href="#about" class="mt-8 inline-flex items-center gap-2 rounded-xl bg-[#E11D48] px-6 py-3 text-base font-semibold text-white shadow-lg transition-all hover:scale-105 hover:bg-[#be123c]">
            협업 제안
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- 4. 실전 영상 리뷰 섹션 -->
  <section id="youtube-long" class="section-spacing bg-[#F8FAFC] py-24">
    <div class="mx-auto max-w-6xl px-4 sm:px-6">
      <h2 class="section-title mb-10 text-2xl font-bold text-[#111827] sm:text-3xl">📺 실전 영상 리뷰</h2>
      <p class="mb-10 text-[#6B7280]">직접 사용하고 비교한 장비 리뷰 영상</p>
      <div id="youtube-long-grid" class="scroll-row flex w-full min-w-0 gap-6 pb-2 pl-4 sm:pl-6 pr-4 sm:pr-6">
        <div class="animate-pulse shrink-0 w-[280px] sm:w-[340px] rounded-lg overflow-hidden bg-gray-200 aspect-video" />
        <div class="animate-pulse shrink-0 w-[280px] sm:w-[340px] rounded-lg overflow-hidden bg-gray-200 aspect-video" />
        <div class="animate-pulse shrink-0 w-[280px] sm:w-[340px] rounded-lg overflow-hidden bg-gray-200 aspect-video" />
      </div>
    </div>
  </section>

  <!-- 5. 빠르게 보는 장비 팁 (Shorts) -->
  <section id="youtube-shorts" class="section-spacing border-t border-slate-200/60 bg-[#f1f5f9] py-24">
    <div class="mx-auto max-w-6xl px-4 sm:px-6">
      <h2 class="section-title mb-10 text-2xl font-bold text-[#111827] sm:text-3xl">🎬 빠르게 보는 장비 팁</h2>
      <div id="youtube-shorts-grid" class="scroll-row flex w-full min-w-0 gap-6 pb-2 pl-4 sm:pl-6 pr-4 sm:pr-6">
        <div class="animate-pulse shrink-0 w-[209px] rounded-lg overflow-hidden bg-gray-200 aspect-[209/314]" />
        <div class="animate-pulse shrink-0 w-[209px] rounded-lg overflow-hidden bg-gray-200 aspect-[209/314]" />
        <div class="animate-pulse shrink-0 w-[209px] rounded-lg overflow-hidden bg-gray-200 aspect-[209/314]" />
      </div>
    </div>
  </section>

  <!-- 6. 장비 비교 & 상세 분석 (블로그) -->
  <section id="blog" class="section-spacing border-t border-slate-200/60 bg-[#F8FAFC] py-24">
    <div class="mx-auto max-w-6xl px-4 sm:px-6">
      <h2 class="section-title mb-10 text-2xl font-bold text-[#111827] sm:text-3xl">✍ 장비 비교 & 상세 분석</h2>
      <div id="blog-grid" class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3 items-stretch">
        <div class="animate-pulse rounded-xl border border-slate-200 bg-white overflow-hidden"><div class="aspect-square bg-gray-200" /><div class="p-4 space-y-2"><div class="h-4 bg-gray-200 rounded w-3/4" /><div class="h-3 bg-gray-200 rounded w-full" /><div class="h-3 bg-gray-200 rounded w-1/2" /></div></div>
        <div class="animate-pulse rounded-xl border border-slate-200 bg-white overflow-hidden"><div class="aspect-square bg-gray-200" /><div class="p-4 space-y-2"><div class="h-4 bg-gray-200 rounded w-3/4" /><div class="h-3 bg-gray-200 rounded w-full" /><div class="h-3 bg-gray-200 rounded w-1/2" /></div></div>
        <div class="animate-pulse rounded-xl border border-slate-200 bg-white overflow-hidden"><div class="aspect-square bg-gray-200" /><div class="p-4 space-y-2"><div class="h-4 bg-gray-200 rounded w-3/4" /><div class="h-3 bg-gray-200 rounded w-full" /><div class="h-3 bg-gray-200 rounded w-1/2" /></div></div>
      </div>
    </div>
  </section>

  <!-- 7. 성과 지표 (Performance Section) -->
  <section class="section-spacing border-t border-slate-200/60 bg-white py-24">
    <div class="mx-auto max-w-6xl px-4 sm:px-6">
      <p class="section-title mb-10 text-center text-sm font-semibold uppercase tracking-wider text-[#6B7280]">누적 성과</p>
      <div class="grid gap-8 sm:grid-cols-3">
        <div class="text-center">
          <p id="stats-youtube-subscribers" class="count-up text-4xl font-bold text-[#E11D48] sm:text-5xl">0</p>
          <p class="mt-2 text-[#6B7280]">유튜브 구독자</p>
        </div>
        <div class="text-center">
          <p id="stats-youtube-views" class="count-up text-4xl font-bold text-[#E11D48] sm:text-5xl">0</p>
          <p class="mt-2 text-[#6B7280]">월간 조회수</p>
        </div>
        <div class="text-center">
          <p id="stats-naver-visitors" class="count-up text-4xl font-bold text-[#E11D48] sm:text-5xl">0</p>
          <p class="mt-2 text-[#6B7280]">누적 방문자</p>
        </div>
      </div>
    </div>
  </section>

  <!-- 8. 강의 티저 섹션 -->
  <section id="lecture" class="section-spacing border-t border-slate-200/60 bg-[#0F172A] py-24">
    <div class="mx-auto max-w-3xl px-4 text-center sm:px-6">
      <h2 class="text-2xl font-bold text-white sm:text-3xl">카메라 입문 & 실전 강의 준비 중</h2>
      <p class="mt-4 text-slate-400">기초부터 실전 촬영까지 단계별 강의 오픈 예정</p>
      <a href="mailto:holaphotographer@gmail.com?subject=강의%20소식%20받기" class="mt-8 inline-flex items-center gap-2 rounded-xl bg-[#E11D48] px-8 py-4 text-base font-semibold text-white shadow-lg transition-all hover:scale-105 hover:bg-[#be123c]">
        강의 소식 받기
      </a>
    </div>
  </section>

  <!-- 9. 협업 / 광고 문의 -->
  <section id="about" class="section-spacing border-t border-slate-200/60 bg-[#F8FAFC] py-24">
    <div class="mx-auto max-w-6xl px-4 sm:px-6">
      <h2 class="section-title mb-10 text-2xl font-bold text-[#111827] sm:text-3xl">협업 및 광고 문의</h2>
      <div class="flex flex-col items-center justify-center gap-6 sm:flex-row sm:gap-6">
        <a href="mailto:holaphotographer@gmail.com" class="flex items-center gap-2 text-[#E11D48] font-semibold hover:underline">holaphotographer@gmail.com</a>
        <a href="https://www.instagram.com/snap.hola" target="_blank" rel="noopener noreferrer" class="flex items-center gap-2 text-[#111827] font-semibold hover:text-[#E11D48]">인스타그램</a>
        <a href="https://youtube.com/channel/UCsD5VP6TvRMt-sq0q25HlIA" target="_blank" rel="noopener noreferrer" class="flex items-center gap-2 text-[#111827] font-semibold hover:text-[#E11D48]">유튜브</a>
      </div>
    </div>
  </section>
</Layout>

<script>
  import { fetchNaverRss, fetchYoutubeLatest, fetchChannelStats, getRssThumbnailUrl, type NaverRssItem, type YoutubeVideoItem } from '../lib/api';

  const youtubeLongGrid = document.getElementById('youtube-long-grid')!;
  const youtubeShortsGrid = document.getElementById('youtube-shorts-grid')!;
  const blogGrid = document.getElementById('blog-grid')!;
  const LIMIT = 6;

  function formatCount(n: string | number): string {
    const num = typeof n === 'string' ? parseInt(n, 10) : n;
    if (isNaN(num)) return '—';
    if (num >= 100000000) return (num / 100000000).toFixed(1).replace(/\.0$/, '') + '억';
    if (num >= 10000) return (num / 10000).toFixed(1).replace(/\.0$/, '') + '만';
    return num.toLocaleString();
  }

  function formatVideoDate(iso: string): string {
    if (!iso) return '';
    const d = new Date(iso);
    if (isNaN(d.getTime())) return '';
    return `${d.getMonth() + 1}월 ${d.getDate()}일`;
  }

  function escapeHtml(s: string) {
    const div = document.createElement('div');
    div.textContent = s;
    return div.innerHTML;
  }

  function stripHtml(html: string): string {
    const div = document.createElement('div');
    div.innerHTML = html;
    return (div.textContent || div.innerText || '').trim();
  }

  function animateCount(el: HTMLElement, target: number, duration = 1500) {
    const start = 0;
    const startTime = performance.now();
    function update(currentTime: number) {
      const elapsed = currentTime - startTime;
      const progress = Math.min(elapsed / duration, 1);
      const t = 1 - Math.pow(1 - progress, 3);
      const current = Math.floor(start + (target - start) * t);
      el.textContent = formatCount(current);
      if (progress < 1) requestAnimationFrame(update);
    }
    requestAnimationFrame(update);
  }

  function renderRssCard(item: NaverRssItem, options?: { priority?: 'high' | 'low' }) {
    const imgUrl = getRssThumbnailUrl(item);
    const fetchPriority = options?.priority === 'high' ? ' fetchpriority="high"' : '';
    const thumb = imgUrl ? `<img src="${escapeHtml(imgUrl)}" alt="${escapeHtml(item.title)}" class="aspect-square w-full object-cover opacity-0 transition-opacity duration-300" loading="lazy"${fetchPriority} decoding="async" onload="this.classList.add('opacity-100')" />` : '<div class="aspect-square bg-gray-200" />';
    const desc = stripHtml(item.description).slice(0, 100) + (stripHtml(item.description).length > 100 ? '...' : '');
    return `
      <article class="blog-card flex h-full flex-col overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm transition-all duration-300 hover:-translate-y-1 hover:shadow-lg">
        <a href="${escapeHtml(item.link)}" target="_blank" rel="noopener" class="flex min-h-0 min-w-0 flex-1 flex-col">
          <div class="overflow-hidden rounded-t-xl shrink-0">${thumb}</div>
          <div class="flex min-h-0 flex-1 flex-col p-4">
            <h3 class="font-semibold line-clamp-2 break-words text-[#111827]">${escapeHtml(item.title)}</h3>
            <p class="mt-1.5 line-clamp-2 break-words text-sm text-[#6B7280]">${escapeHtml(desc)}</p>
          </div>
        </a>
      </article>
    `;
  }

  function renderYoutubeCard(v: YoutubeVideoItem, options?: { priority?: 'high' | 'low'; label?: string; isShort?: boolean }) {
    const fetchPriority = options?.priority === 'high' ? ' fetchpriority="high"' : '';
    const label = options?.label ?? '유튜브 보기 →';
    const cardWidth = options?.isShort ? 'w-[209px]' : 'w-[280px] sm:w-[340px]';
    const viewStr = v.viewCount ? formatCount(v.viewCount) + ' 회' : '';
    const dateStr = formatVideoDate(v.publishedAt);
    const metaStr = [viewStr, dateStr].filter(Boolean).join(' · ');
    let thumb: string;
    if (options?.isShort) {
      const aspect = 'aspect-[209/314]';
      thumb = v.thumbnail_url
        ? `<div class="${aspect} w-full overflow-hidden bg-gray-200"><img src="${escapeHtml(v.thumbnail_url)}" alt="${escapeHtml(v.title)}" class="h-full w-full object-cover object-center opacity-0 transition-all duration-300 group-hover:scale-105" loading="lazy"${fetchPriority} decoding="async" onload="this.classList.add('opacity-100')" /></div>`
        : `<div class="${aspect} w-full bg-gray-200" />`;
    } else {
      const aspect = 'aspect-video';
      thumb = v.thumbnail_url
        ? `<div class="${aspect} w-full overflow-hidden bg-gray-200"><img src="${escapeHtml(v.thumbnail_url)}" alt="${escapeHtml(v.title)}" class="h-full w-full object-cover opacity-0 transition-all duration-300 group-hover:scale-105" loading="lazy"${fetchPriority} decoding="async" onload="this.classList.add('opacity-100')" /></div>`
        : `<div class="${aspect} bg-gray-200" />`;
    }
    return `
      <a href="${escapeHtml(v.link)}" target="_blank" rel="noopener" class="yt-card group scroll-row-item shrink-0 ${cardWidth} rounded-lg overflow-hidden bg-white shadow-md transition-all duration-300 hover:-translate-y-1 hover:scale-[1.02] hover:shadow-xl active:scale-[0.98]">
        ${thumb}
        ${metaStr ? `<div class="px-4 py-2 bg-gray-50 border-b border-gray-100"><p class="text-xs text-[#6B7280]">${escapeHtml(metaStr)}</p></div>` : ''}
        <div class="p-4 bg-white">
          <h3 class="font-semibold line-clamp-2 leading-relaxed text-sm sm:text-base text-[#111827]">${escapeHtml(v.title)}</h3>
          <p class="mt-1 text-xs text-[#E11D48] font-medium">${escapeHtml(label)}</p>
        </div>
      </a>
    `;
  }

  function renderRssSection(grid: HTMLElement, items: NaverRssItem[], emptyMsg: string, priority: 'high' | 'low' = 'low') {
    if (items.length === 0) {
      grid.innerHTML = `<p class="col-span-full text-center text-[#6B7280]">${emptyMsg}</p>`;
    } else {
      const toShow = items.slice(0, LIMIT);
      grid.innerHTML = toShow.map((item, i) => renderRssCard(item, { priority: priority === 'high' && i < 3 ? 'high' : 'low' })).join('');
    }
  }

  function renderYoutubeSection(grid: HTMLElement, items: YoutubeVideoItem[], emptyMsg: string, label: string, isShort = false) {
    if (items.length === 0) {
      grid.innerHTML = `<p class="shrink-0 px-4 sm:px-6 text-[#6B7280]">${emptyMsg}</p>`;
    } else {
      const toShow = items.slice(0, LIMIT);
      grid.innerHTML = toShow.map((v, i) => renderYoutubeCard(v, { priority: i < 3 ? 'high' : 'low', label, isShort })).join('');
    }
  }

  async function load() {
    try {
      const [rssRes, youtubeRes, statsRes] = await Promise.all([
        fetchNaverRss(),
        fetchYoutubeLatest(),
        fetchChannelStats().catch(() => null),
      ]);

      if (statsRes) {
        const subEl = document.getElementById('stats-youtube-subscribers');
        const viewsEl = document.getElementById('stats-youtube-views');
        const naverEl = document.getElementById('stats-naver-visitors');
        const expertSubs = document.getElementById('stats-expert-subs');
        const expertBlog = document.getElementById('stats-expert-blog');

        const subs = parseInt(statsRes.youtubeSubscribers ?? '0', 10);
        const views = parseInt(statsRes.youtubeViewCount ?? '0', 10);
        const naver = parseInt(statsRes.naverBlogVisitors ?? '0', 10);

        if (subEl && !isNaN(subs)) animateCount(subEl, subs);
        if (viewsEl && !isNaN(views)) viewsEl.textContent = formatCount(views);
        if (naverEl && !isNaN(naver)) animateCount(naverEl, naver);
        if (expertSubs) expertSubs.textContent = formatCount(subs);
        if (expertBlog) expertBlog.textContent = statsRes.naverBlogVisitors ? formatCount(statsRes.naverBlogVisitors) + '+' : '65만+';
      }

      const videos = youtubeRes.videos || [];
      const shorts = youtubeRes.shorts || [];
      const youtubeError = youtubeRes.error;

      renderYoutubeSection(youtubeLongGrid, videos, youtubeError ? '유튜브 API 키를 설정해주세요.' : '등록된 롱폼 영상이 없습니다.', '유튜브 보기 →');
      setTimeout(() => {
        renderYoutubeSection(youtubeShortsGrid, shorts, youtubeError ? '유튜브 API 키를 설정해주세요.' : '등록된 Shorts가 없습니다.', 'Shorts 보기 →', true);
      }, 80);

      const allBlog = [...rssRes.reviews, ...rssRes.guides, ...rssRes.models]
        .filter((item) => !item.title.includes('보금자리론'))
        .sort((a, b) => new Date(b.pubDate).getTime() - new Date(a.pubDate).getTime())
        .slice(0, 6);
      setTimeout(() => {
        renderRssSection(blogGrid, allBlog, '등록된 블로그 글이 없습니다.', 'high');
      }, 160);
    } catch (e) {
      youtubeLongGrid.innerHTML = `<p class="col-span-full text-center text-[#E11D48]">영상을 불러올 수 없습니다</p>`;
      youtubeShortsGrid.innerHTML = '<p class="col-span-full text-center text-[#E11D48]">Shorts를 불러올 수 없습니다</p>';
      blogGrid.innerHTML = '<p class="col-span-full text-center text-[#E11D48]">블로그를 불러올 수 없습니다</p>';
    }
  }

  load();
</script>
